<div>
  <div class="w-50 mx-auto mt-5">
    <h1>Are you sure you want to delete this?</h1>
    <h2>This can NOT be undone!</h2>
    <p>This will also delete all the students within this class!</p>
  </div>
  <div class="w-50 mx-auto">
    <h4><%= @grade.gradYear %></h4>
    <p><%= @grade.classAdvisorOne %></p>
    <p><%= @grade.classAdvisorTwo%></p>
  </div>
  <div class="w-50 mx-auto">
      <%= form_with model: @grade, class: "nifty_form", url: grade_url(:id => @grade.id), method: :delete do |f| %>
      <p><%= f.submit "Yes", class: "bg-success text-light rounded p-1"%> <%=link_to "No", grades_url %></p>
<% end %>
  </div>
</div>
